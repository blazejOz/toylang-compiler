fn factorial(int n): int {
    int res = 1;
    int t = n;
    while (t > 1) {
        res = res * t;
        t = t - 1;
    }
    return res;
}

fn main(): int {
    print(factorial(5));
    return 0;
}